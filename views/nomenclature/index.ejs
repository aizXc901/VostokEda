<h2>Nomenclature</h2>

<div class="mb-3">
    <a href="/nomenclature/new" class="btn btn-primary">Add New Item</a>
</div>

<div class="row">
    <div class="col-md-12">
        <% if (nomenclatures.length === 0) { %>
            <div class="alert alert-info">No nomenclature items found. <a href="/nomenclature/new">Create one now</a>.</div>
        <% } else { %>
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Category</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% nomenclatures.forEach(item => { %>
                        <tr>
                            <td><%= item.id %></td>
                            <td><%= item.name %></td>
                            <td><%= item.CostCategory.name %></td>
                            <td>
                                <!-- Add edit/delete buttons if needed -->
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } %>
    </div>
</div>